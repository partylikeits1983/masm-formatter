# comment
export.someLib::somefunction
# comment

# code comment
export.function
    # comment should be indented
    debug.stack
    push.1.2.3.4
    # => [stack, comment]

    push.1.2.3.4
    # => [stack, comment]

    push.1.2.3.4
    # => [stack, comment]
    # => will have empty line after

    dropw
    #! => will not have empty line after
    # not a stack comment
    # => [stack, comment]

    if.true
        while.true
            # comment
            add sub
            # => [stack, comment]

            dropw
            # => [stack, comment]
        end
    end
    exec.function::some_func
    # comment
end

proc.timestamp
    # comment should be indented
    blockstamp
    if.true
        add
    else
        sub
    end
    # comment
end
